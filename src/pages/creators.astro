---
/**
 * /creators — Phase C (top1pct_1105) redirect stub.
 *
 * The Astro config `redirects: { '/creators': '/referrals' }` emits a
 * meta-refresh at build time. This file is kept as a belt-and-suspenders
 * SSR fallback using Astro.redirect() for any server-rendered paths.
 *
 * In production (Caddy on wisechef-hq), also add to /etc/caddy/Caddyfile:
 *   redir /creators /referrals 301
 */
return Astro.redirect('/referrals', 301);
---
