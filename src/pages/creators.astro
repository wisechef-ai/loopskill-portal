---
/**
 * /creators — compat redirect (Phase H, recipes_2005 sprint).
 *
 * Kept as compat redirect; nav now points directly at /referrals.
 * Any external link pointing at /creators still works via this 302.
 *
 * In production (Caddy on wisechef-hq), also add to /etc/caddy/Caddyfile:
 *   redir /creators /referrals 302
 */
return Astro.redirect('/referrals', 302);
---
