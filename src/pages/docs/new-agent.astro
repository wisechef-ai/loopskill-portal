---
import AppShell from '../../layouts/AppShell.astro';
---

<AppShell mode="public" title="Adding a new agent runtime — LoopSkill Docs"
      description="Install the LoopSkill meta-skill on Hermes, Claude Code, Claude Desktop, Codex, Cursor, Cline, Continue.dev, Zed, OpenClaw, or any custom agent.">
  <section class="max-w-3xl mx-auto px-6 pt-16 pb-12">
    <p class="eyebrow mb-2">Documentation · Integrations</p>
    <h1 class="text-4xl font-semibold tracking-tight mb-6">Adding LoopSkill to your agent</h1>
    <div class="prose">
      <p>
        LoopSkill supports 10 agent runtimes out of the box. Pick your runtime below for the
        exact install command and config snippet. All paths need your
        <code>LOOPSKILL_API_KEY</code> environment variable set.
        See <a href="/docs/getting-started">Getting started</a> for key setup.
      </p>
      <p>
        For a visual overview of all integrations, see the
        <a href="/integrations">Integrations page</a>.
      </p>

      <!-- ─────────────── NATIVE (SKILL.md drop) ─────────────── -->
      <h2>Native skill drop (no MCP server needed)</h2>
      <p>
        These runtimes auto-discover <code>SKILL.md</code> files. Drop the file in once;
        the agent picks it up automatically on next start.
      </p>

      <h3>Hermes</h3>
      <p>Hermes reads skills from <code>~/.hermes/skills/</code>. After install, the
      <code>loopskill_search</code>, <code>loopskill_install</code>, and <code>loopskill_sync</code>
      MCP tools are available automatically via the LoopSkill MCP server bundled in the skill.</p>
      <pre><code>mkdir -p ~/.hermes/skills/loopskill
curl -sL https://app.loopskill.io/skill \
     -o ~/.hermes/skills/loopskill/SKILL.md
export LOOPSKILL_API_KEY=rec_xx...</code></pre>

      <h3>Claude Code</h3>
      <p>Claude Code reads skills from <code>~/.claude/skills/</code>:</p>
      <pre><code>mkdir -p ~/.claude/skills/loopskill
curl -sL https://app.loopskill.io/skill \
     -o ~/.claude/skills/loopskill/SKILL.md
export LOOPSKILL_API_KEY=rec_xx...</code></pre>

      <h3>OpenClaw</h3>
      <p>OpenClaw mounts the same skill directory layout as Claude Code. Drop the meta-skill in once;
      every spawned agent inherits it:</p>
      <pre><code>mkdir -p ~/.openclaw/skills/loopskill
curl -sL https://app.loopskill.io/skill \
     -o ~/.openclaw/skills/loopskill/SKILL.md
export LOOPSKILL_API_KEY=rec_xx...</code></pre>

      <h3>Codex CLI</h3>
      <p>OpenAI Codex CLI uses the same format:</p>
      <pre><code>mkdir -p ~/.codex/skills/loopskill
curl -sL https://app.loopskill.io/skill \
     -o ~/.codex/skills/loopskill/SKILL.md
export LOOPSKILL_API_KEY=rec_xx...</code></pre>

      <!-- ─────────────── MCP (config snippet) ─────────────── -->
      <h2>MCP server integrations</h2>
      <p>
        These runtimes use the <a href="https://modelcontextprotocol.io">Model Context Protocol</a>.
        LoopSkill is a <strong>hosted</strong> MCP server — no local install, no clone, no <code>pip install</code>.
        Point your client at the StreamableHTTP endpoint with your key in the <code>x-api-key</code> header
        and your agent gets <code>loopskill_search</code>, <code>loopskill_install</code>,
        <code>loopskill_sync</code>, and all other LoopSkill tools.
      </p>
      <pre><code>{`{
  "mcpServers": {
    "loopskill": {
      "url": "https://app.loopskill.io/api/mcp/http/",
      "headers": { "x-api-key": "rec_xx..." }
    }
  }
}`}</code></pre>

      <h3>Claude Desktop</h3>
      <p>Edit <code>~/Library/Application Support/Claude/claude_desktop_config.json</code>
      (macOS) or <code>%APPDATA%\Claude\claude_desktop_config.json</code> (Windows) with the config above.</p>
      <p>Restart Claude Desktop. The LoopSkill tools appear in the tool picker.</p>

      <h3>Cursor</h3>
      <p>Open Cursor Settings → MCP → Add Server, or edit <code>.cursor/mcp.json</code> with the same config.</p>

      <h3>Cline (VS Code extension)</h3>
      <p>In VS Code, open Cline settings → MCP Servers → Edit JSON with the same config.</p>
      <p>Skills installed via the <code>loopskill_install</code> tool are written to
      <code>~/.claude/skills/</code> by default; Cline picks them up.</p>

      <h3>Continue.dev</h3>
      <p>Edit <code>~/.continue/config.json</code>:</p>
      <pre><code>{`{
  "experimental": {
    "modelContextProtocolServers": [
      {
        "transport": {
          "type": "streamable-http",
          "url": "https://app.loopskill.io/api/mcp/http/",
          "headers": { "x-api-key": "rec_xx..." }
        }
      }
    ]
  }
}`}</code></pre>

      <h3>Zed</h3>
      <p>Edit <code>~/.config/zed/settings.json</code>:</p>
      <pre><code>{`{
  "context_servers": {
    "loopskill": {
      "source": "custom",
      "url": "https://app.loopskill.io/api/mcp/http/",
      "headers": { "x-api-key": "rec_xx..." }
    }
  }
}`}</code></pre>

      <!-- ─────────────── REST (any agent) ─────────────── -->
      <h2>Any agent (REST API)</h2>
      <p>
        If your agent can make HTTP requests, you can use the LoopSkill REST API directly.
        No MCP server required.
      </p>
      <pre><code>{`# Search
curl -H "x-api-key: $RECIPES_API_KEY" \
  "https://app.loopskill.io/api/skills/search?q=seo"

# Install (get signed tarball URL)
curl -H "x-api-key: $RECIPES_API_KEY" \
  "https://app.loopskill.io/api/skills/install?slug=super-memory&mode=files"`}</code></pre>
      <p>See <a href="/docs/api-reference">API reference</a> for full endpoint docs.</p>

      <!-- ─────────────── TROUBLESHOOTING ─────────────── -->
      <h2>Common troubleshooting</h2>
      <ul>
        <li>
          <strong>"Skills install but agent ignores them"</strong> — the agent's skills
          directory is non-standard. Check the agent's docs and copy the installed files there
          manually, or set <code>LOOPSKILL_INSTALL_DIR</code> to the correct path.
        </li>
        <li>
          <strong>"loopskill_install tool not found"</strong> — the MCP server isn't configured
          or started. Re-check your config file and restart the agent.
        </li>
        <li>
          <strong>"401 Unauthorized"</strong> — your <code>LOOPSKILL_API_KEY</code> env var isn't
          being passed to the MCP server process. Check the <code>env</code> block in your config.
        </li>
        <li>
          <strong>"403 Subscription required"</strong> — the skill requires a Cook or Operator
          subscription. Check <a href="/pricing">pricing</a> or install a free-tier skill first
          (e.g. <code>super-memory</code>).
        </li>
      </ul>

      <h2>Building a custom runtime profile</h2>
      <p>
        If your agent isn't listed here, it likely accepts one of the two patterns above:
        a skill directory drop (if it follows the Claude Code SKILL.md standard) or an MCP
        server config. If neither works, contact us — we add new native profiles when two
        operators validate them on different machines.
      </p>
    </div>
  </section>
</AppShell>
