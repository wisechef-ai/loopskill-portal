---
// M4 (autopilot_0308) — this page documented a flow that does not exist.
//
// It walked a reader step by step through a bundle-to-client-agent flow behind
// a "coming next quarter" banner naming a quarter that was already four months
// past when this was cut on 2026-08-03. The underlying surface never worked:
// `bundle_deployments = 0`, and loopskill-api
// app/bundle_deployment_routes.py:326,348 returns a permanent
// `{"status":"applying"}` with no terminal state and no code path that reaches
// one. hub §5 carries it as ❌ "Deploy to a fleet member".
//
// Softening the banner was not an option — a hedge is still a claim, and an
// expired hedge is just a claim. What IS real (bundle-scoped API keys, a client
// agent converging to a bundle on its own poll cycle) is documented honestly on
// /docs/cookbooks, which is where this now points.
//
// Kept as a redirect so no inbound link or search result 404s.
// Tombstone: ~/.hermes/state/tombstones/portal-docs-deployment.deleted-20260803.astro
// Ledger:    ~/.hermes/state/deletion-ledger.tsv (2026-08-03)
import RedirectStub from '../../components/RedirectStub.astro';
---
<RedirectStub to="/docs/cookbooks" title="Bundles" />
